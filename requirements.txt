# =============================================================================
# FastAPI V3.0 Standard Project - 锁定依赖清单
# 版本与 pyproject.toml 最低要求对齐
# =============================================================================

# FastAPI & Server
fastapi>=0.121.0
uvicorn[standard]>=0.38.0

# Database
sqlalchemy[asyncio]>=2.0.44
asyncpg>=0.30.0
alembic>=1.17.0
psycopg[binary]>=3.1.0       # Alembic 同步迁移驱动 (Psycopg 3)

# Pydantic
pydantic>=2.12.0
pydantic-settings>=2.12.0
email-validator>=2.1.0        # Pydantic EmailStr 校验

# Logging
loguru>=0.7.2

# UUID v7
uuid6>=2024.1.12

# JSON Serialization
orjson>=3.10.0

# Security
pwdlib[argon2]>=0.2.0         # 密码哈希 (Argon2id)
PyJWT[crypto]>=2.8.0              # JWT 签发与验签 (替代 python-jose)
python-multipart>=0.0.12      # Form data 支持

# Redis
redis>=5.0.0                  # 异步 Redis 客户端

# Development & Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.6.0
mypy>=1.10.0
pre-commit>=3.7.0
